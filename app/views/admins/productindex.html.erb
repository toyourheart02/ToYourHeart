
<div class="col-md-10 col-md-offset-1 ">
	<div class="title">
		<h3>商品一覧ページ</h3>
	</div>
	<% @products.each do |product| %>
	<div class="col-md-5 col-md-offset-1">
		<table class="table">
			<tbody>

				<%= attachment_image_tag product, :music_image,  format: 'jpeg', class: "", fallback: "no_image.jpg", size: "150x150" %>

				<% if  product.is_deleted == true  %>
				<span class="sakujyo">削除済み</span>
				<% end %>

				<tr>
					<td>タイトル</td>
					<td><%= product.title %></td>
				</tr>
				<tr>
					<td>アーティスト</td>
					<td><%= product.artist.artist_name %></td>
				</tr>
				<tr>
					<td>レーベル</td>
					<td><%= product.label.label_name %></td>
				</tr>
				<tr>
					<td>ジャンル</td>
					<td><%= product.genre.genre_name %></td>
				</tr>
				<tr>
					<td>価格</td>
					<td><%= product.price %></td>
				</tr>
				<tr>
					<td>在庫数</td>
					<td><%= product.stock %></td>
				</tr>
				<tr>
					<td><button type="button" class="btn btn-success"><%=link_to "編集", product_path(product) %></button></td>
					<td><button type="button" class="btn btn-danger"><%= link_to "削除", admins_destroy_path(product), method: :delete, "data-confirm" => "本当に削除しますか？" %></button></td>
				</tr>
			</tbody>
		</table>
	</div>
	<% end %>
</div>

<style>
.sakujyo{
	background-color: black;
	color: white;
	margin-left: 100px;
	padding: 10px;
}
</style>